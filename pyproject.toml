[project]
name = "ldi-pyspark-2026"
version = "0.1.0"
description = "LAB SPARK LDI Training Mars 2026"
requires-python = ">=3.10"
readme = "README.md"

dependencies = [
    "pyspark==3.5.5",
]

[project.optional-dependencies]
dev = [
]

[tool.uv]
